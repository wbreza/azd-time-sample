# yaml-language-server: $schema=https://raw.githubusercontent.com/Azure/azure-dev/main/schemas/v1.0/azure.yaml.json

name: azd-time-sample
metadata:
  template: azd-time-sample
services:
  react-appservice:
    project: ./src/sample
    dist: build
    language: js
    host: appservice
  # react-containerapp:
  #   project: ./src/sample
  #   dist: build
  #   language: js
  #   host: containerapp
  react-staticwebapp:
    project: ./src/sample
    dist: build
    language: js
    host: staticwebapp
